<div class="modal-dialog modal-lg modal-dialog-center">
  <div class="modal-content">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
      <h1 class="modal-title">質問への回答</h1>
    </div>
    <div class="modal-body">
      <div class="row">
        <div class="col-md-6 col-md-offset-3">
          
          
          <%= form_with(model: @user, url: inquiries_update_answer_user_path, local: true, method: :patch) do |f| %>
          　<% @inquiries.each do |inquiry| %>
          　 <%= f.fields_for "inquiries[]", inquiry do |inf| %>
            
            <%= inf.label :回答, class: "label-#{yield(:class_text)}" %>
            <%= inf.text_area :answer, class: "form-control" %>
          
            <div class="center">
              <%= f.submit "送信する", class: "btn btn-primary" %>
              <button type="button" class="btn btn-default btn-<%= yield(:class_text) %>" data-dismiss="modal">
                キャンセル
              </button>
            </div>
            <% end %>
            <% end %>
            
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>